import ExtLinks from "./ExtLinks"
import ProjectGrid from "./ProjectsGrid"

export default function Main() {
    return (
        <main>
            <ExtLinks />
            <ProjectGrid />
        </main>
    )
}